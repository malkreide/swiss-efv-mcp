{
  "SEC-004": {"status":"partial","category":"SEC","severity":"critical","evidence":["client.py:41-74 — all three dataset URLs are hardcoded module constants (https:// on data.finance.admin.ch / efv.admin.ch); no user input ever builds a URL, so the primary SSRF vector is structurally absent","client.py:122-123 — httpx.AsyncClient uses default TLS verification (never disabled) but follow_redirects=True","SECURITY.md:22 & README.md:180-182 — documents fixed hardcoded egress, 'no SSRF surface'"],"gaps":["No explicit HTTPS scheme validation before requests (no urlparse scheme check)","No resolved-IP blocklist (169.254.169.254, private/loopback/link-local ranges never checked)","No DNS pinning / getaddrinfo guard","follow_redirects=True means an upstream 3xx could redirect the fetch to an internal/metadata IP with no post-resolution IP check — residual SSRF-via-redirect gap"]},
  "SEC-005": {"status":"partial","category":"SEC","severity":"high","evidence":["client.py:42,58,64,70 — hostnames are fixed trusted EFV constants, not user-controlled, so an attacker cannot introduce a rebinding domain","client.py:122-123 — httpx default client, no custom transport, no DNS pinning; follow_redirects=True"],"gaps":["No DNS-Pinning: no getaddrinfo/ipaddress single-resolution logic (grep confirms neither symbol present in src/)","httpx default performs implicit resolution with no pinned-IP reuse (TOCTOU window exists in principle)","No SNI/Host-header preservation logic because no pinning implemented","follow_redirects=True could follow a redirect to a rebound host without re-validation"]},
  "SEC-006": {"status":"pass","category":"SEC","severity":"high","evidence":["__main__.py:23 — transport = os.environ.get('TRANSPORT', 'stdio').lower(); default is stdio","__main__.py:25-30 — SSE/streamable-http only via explicit TRANSPORT env var; stdio path opens no port","README.md:78-102 — local stdio + Claude Desktop documented as primary use; cloud SSE documented separately with HOST guidance"],"gaps":["Runtime port-open test not executed in this static audit (design strongly indicates no port on default start)"]},
  "SEC-007": {"status":"partial","category":"SEC","severity":"high","evidence":["Dockerfile:12 — useradd --uid 10001 non-root user (UID >= 10000)","Dockerfile:14 — USER 10001 set before ENTRYPOINT","Dockerfile:4-10 — multi-stage build (build stage + slim runtime)"],"gaps":["No readOnlyRootFilesystem (no k8s manifests / no runtime read-only enforcement)","No capabilities drop (CapDrop ALL) and no seccomp profile referenced","No Kubernetes SecurityContext (runAsNonRoot/allowPrivilegeEscalation:false) — no k8s/helm manifests in repo","No container security scan (Trivy/Snyk) step in CI"]},
  "SEC-008": {"status":"pass","category":"SEC","severity":"medium","evidence":["pyproject.toml:1-3,40-41 — hatchling build backend, only a regular [project.scripts] entry point; no pre/postinstall hooks or custom cmdclass","README.md:59-76 — full untruncated install command (uvx swiss-efv-mcp / pip install) and complete claude_desktop_config.json shown transparently","publish.yml:37-55 — PyPI publish via OIDC (permissions id-token: write, environment pypi, pypa/gh-action-pypi-publish) = Trusted Publisher with Sigstore; CONTRIBUTING.md/PUBLISHING.md document the build/release process"],"gaps":["README does not show an explicit end-user sigstore signature-verification snippet (optional)"]},
  "SEC-009": {"status":"todo","category":"SEC","severity":"critical","evidence":["Server is no-auth public-open-data: no session-management, user-identity, or Mcp-Session-Id binding code anywhere in src/ (server.py has no auth/session logic)","SECURITY.md:38-40 — explicitly documents session crypto-binding as not applicable ('no user identity to bind, as the server exposes public data with no authentication')","SECURITY.md:53-56 — re-evaluation trigger: implement bound/TTL'd/server-invalidated session IDs if an auth model is ever added"],"gaps":["No custom session binding implemented — structurally N/A because there is no auth model / user identity; FastMCP handles any transport session id internally. Needs manual confirmation that the documented N/A rationale is accepted for the dual-transport (SSE) deployment"]},
  "SEC-013": {"status":"pass","category":"SEC","severity":"high","evidence":["No secrets/API keys anywhere: grep of src/ finds no KEY/SECRET/TOKEN env reads; endpoints are unauthenticated public OGD","SECURITY.md:24 — 'Unauthenticated public OGD — no API keys, tokens or secrets are stored or forwarded'; README.md:184-186 & CONTRIBUTING.md:15-16 confirm no credential handling","Dockerfile:1-22 — no secret ENV layers baked into image (only TRANSPORT/HOST/PORT/PYTHONUNBUFFERED)"],"gaps":["None — the server uses zero credentials (best case; Stufe-1 documentation requirement is exceeded since there is no secret at all)"]},
  "SEC-014": {"status":"todo","category":"SEC","severity":"medium","evidence":["Single standalone read-only server; no per-team tool allow-list config in repo (no gateway-config/allowlist files)","SECURITY.md:57-59 — tool allow-listing explicitly deferred to the MCP gateway/host layer ('enable the gateway's tool allow-listing') as an accepted portfolio-level control","No auth model exists, so server-side group/role checks are not applicable (no OAuth group claims)"],"gaps":["Gateway-layer control, out of scope for a single no-auth server; needs manual portfolio-level judgment on whether a gateway with per-team allow-lists is deployed in front"]},
  "SEC-015": {"status":"todo","category":"SEC","severity":"medium","evidence":["server.py:190-248 — all five tools are statically authored in-repo via @mcp.tool(); no dynamic or remote tool registration anywhere","SECURITY.md:36-41 — tool-poisoning detection documented as gateway/host responsibility; tool definitions are version-controlled, in-repo, PR-reviewed","No external MCP servers are aggregated by this server (single-server, own-tools-only → low residual risk per the check's own scoping)"],"gaps":["No pre-flight poisoning detection layer (correctly a gateway responsibility, not a single-server one); needs manual judgment tied to whether untrusted external servers are ever integrated behind a shared gateway"]},
  "SEC-016": {"status":"pass","category":"SEC","severity":"critical","evidence":["__main__.py:26 — mcp.settings.host = os.environ.get('HOST', '127.0.0.1'); loopback default, no 0.0.0.0 hardcoded in code","Dockerfile:16-18 — HOST=0.0.0.0 set explicitly and only in the container ENV (container-scoped opt-in)","README.md:95 & 190-191 and SECURITY.md:29 — document loopback default and that 0.0.0.0 is a container-only explicit opt-in (grep finds 0.0.0.0 only in Dockerfile, never in src/)"],"gaps":["Optional runtime warning when binding 0.0.0.0 outside a container is not implemented (explicitly optional in the check)"]},
  "SEC-018": {"status":"partial","category":"SEC","severity":"high","evidence":["server.py:191-248 — FastMCP derives an input schema from the typed signatures, so type-level validation exists (e.g. year_from: int | None is enforced as int; a non-int LLM value is rejected at the boundary)","server.py:191-233 — tool args (variable: str, household: str, level: int, contains: str|None) only filter already-cached in-memory rows; they never build a URL, reach a subprocess, or hit SQL (harmless sink)","models.py:19-88 — Pydantic v2 envelopes exist but are response models, not input-constraint schemas"],"gaps":["No numeric ge/le constraints on int args (level, year_from, year_to) — LLM can pass arbitrary/negative/huge ints","No str min_length/max_length or whitelist pattern on string args (variable, household, model, topic, contains, departement)","No Pydantic strict=True and no extra='forbid' on any input model","SECURITY.md:25 overstates this as full 'Pydantic v2 validation at all tool boundaries' — only type validation is present, not constraint validation"]},
  "SEC-019": {"status":"pass","category":"SEC","severity":"critical","evidence":["server.py:190-248 & README.md:114-116 — all tools are read-only (HTTP GET only); no write, send, email, webhook, or filesystem capability (grep finds no smtplib/requests.post/httpx.post/slack/webhook)","Trifecta score ~1/3: private data ❌ (public OGD only), external send ❌ (read-only), untrusted content limited to fixed trusted EFV CSV hosts (client.py:41-74)","README.md:177-220 (Safety & Limits / Project Phase) and SECURITY.md:5,15-18 thoroughly document read-only-by-design and require re-audit before any write tool is added"],"gaps":["No section explicitly titled 'Lethal Trifecta assessment' with the 3-capability table, though the substantive assessment is fully documented across README Safety & Limits + Project Phase"]},
  "SEC-020": {"status":"pass","category":"SEC","severity":"critical","evidence":["grep across src/ finds no os.system, shell=True, eval(, exec(, os.popen, or subprocess — zero shell/subprocess/eval usage","client.py:97-136 — only httpx GET + csv.DictReader parsing; no external binary invocation","server.py:37-248 — pure in-memory row filtering; no filesystem or command construction from tool args"],"gaps":["None"]},
  "SEC-021": {"status":"partial","category":"SEC","severity":"high","evidence":["client.py:41-74 — egress is de-facto fixed to three hardcoded https constants on two EFV hosts; no user input can introduce a new host (stronger than a mutable allow-list in that dimension)","README.md:180-182 & SECURITY.md:22 — the two allowed EFV hosts and fixed-egress posture are documented","client.py:122-123 — single httpx client; TLS verification on"],"gaps":["No explicit code-layer allow-list: no frozenset ALLOWED_HOSTS and no assert_host_allowed() pre-request guard (grep finds neither)","No network-layer egress control (no k8s NetworkPolicy, no docs/network-egress.md, no security-group rules)","follow_redirects=True (client.py:123) permits a redirect to leave the fixed EFV hosts with no host re-check","No documented update procedure for changing the egress set"]},
  "SEC-022": {"status":"partial","category":"SEC","severity":"high","evidence":["server.py:190-241 — 4 of 5 tools carry a topic prefix (fiscal_headline, fiscal_budget_breakdown, fiscal_by_institution, fiscal_list_dimensions), reducing collision risk","SECURITY.md:38-41 — tool definitions are version-controlled, authored in-repo, PR-reviewed, with no dynamic/remote registration (partial rug-pull mitigation)","server.py:245 — dump_status has no prefix at all; no server-identity namespace (e.g. swiss_efv__) is used"],"gaps":["No server-identity namespace prefix: 'fiscal_' is a topic prefix, not a <server>__ identity prefix, and is inconsistent (dump_status unprefixed) — cross-server shadowing not structurally prevented","No tool-definition hash snapshot generated at release: publish.yml has no sha256/tool-hashes.json step (grep of CHANGELOG/server.json finds no hash/namespace mention)","No CHANGELOG discipline for tool-definition changes / re-approval hints"]}
}
